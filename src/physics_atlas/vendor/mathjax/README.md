# MathJax browser bundle

`tex-svg.js` is the self-contained TeX-to-SVG bundle from MathJax 3.2.2,
distributed through the official `mathjax` npm package. Its license is kept in
this directory as `LICENSE`.
