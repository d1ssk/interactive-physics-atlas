const byId = id => document.getElementById(id);
const PLOT_CONFIG = {
  responsive: true,
  scrollZoom: true,
  displaylogo: false,
  displayModeBar: window.matchMedia("(min-width: 520px)").matches ? "hover" : false,
};
const LOCALE = new URLSearchParams(window.location.search).get("lang") === "ja" ? "ja" : "en";

const MESSAGES = {
  en: {
    title: "Two-Dimensional Conformal Transformations",
    lede: "Trace conformal symmetry from local differentials to global Möbius transformations and the infinitesimal Witt algebra.",
    localTitle: "Local Conformality",
    mapLabel: "Map",
    mixingLabel: "Antiholomorphic mixing \\(\\lambda\\)",
    axisRatio: "axis ratio \\(K\\)",
    orientationLabel: "orientation",
    localCaption: "Purple shows the exact image of a small circle; dashed green shows its first-order Wirtinger approximation. For a conformal differential, the two local singular values are equal.",
    globalTitle: "Möbius Transformations",
    mobiusLabel: "\\(\\mathrm{PSL}(2,\\mathbb C)\\) preset",
    sphereLegend: "Gray lines show the reference grid, while blue and gold show the transformed latitude and longitude families. Red points are fixed points of the Möbius transformation.",
    mobiusInvariant: "<strong>Invariant:</strong> generalized circles map to generalized circles, crossing angles are preserved, and the ordered cross ratio is unchanged.",
    algebraTitle: "Witt Algebra",
    modeLabel: "Mode \\(m\\)",
    flowLabel: "Flow parameter \\(\\epsilon\\)",
    wittCaption: "Red arrows show the generator for positive flow parameter. Only \\(m=-1,0,1\\) define globally regular holomorphic vector fields on the Riemann sphere; the other finite flows shown here are local and use a principal branch where needed.",
    chainTitle: "Summary",
    chainLocal: "<strong>Local:</strong> if \\(\\partial_{\\bar z}f=0\\) and \\(\\partial_zf\\ne0\\), the real Jacobian is a rotation followed by a uniform scale.",
    chainGlobal: "<strong>Global:</strong> biholomorphic maps of \\(\\mathbb{CP}^1\\) form \\(\\mathrm{PSL}(2,\\mathbb C)\\).",
    chainAlgebra: "<strong>Algebraic:</strong> \\(\\ell_{-1},\\ell_0,\\ell_1\\) generate the global Möbius subgroup, while all integer modes form the local centerless Witt algebra.",
    orientationPreserving: "preserving",
    orientationReversing: "reversing",
    orientationDegenerate: "degenerate",
    localPlotAria: "A complex-plane grid and a small circle before and after the selected map",
    spherePlotAria: "Reference and transformed coordinate curves on the Riemann sphere",
    wittPlotAria: "A finite Witt flow and its infinitesimal vector field on the complex plane",
    localNotes: {
      square: "Away from \\(z=0\\), the differential is multiplication by \\(2z\\), so a sufficiently small circle remains circular to first order.",
      conjugate: "Complex conjugation preserves angles but reverses local orientation.",
      mixed: "At \\(\\lambda=0\\) the map is holomorphic. As \\(|\\lambda|\\) increases, the two singular values separate, and the differential becomes degenerate at \\(|\\lambda|=1\\).",
      exponential: "The grid bends and overlaps globally, while every nonzero local derivative is still a rotation followed by a uniform scale.",
      "special-conformal": "Away from its pole in the plane coordinate, this is the finite flow generated by \\(\\ell_1=-z^2\\partial_z\\).",
    },
    mobiusNotes: {
      identity: "Every point on the sphere is fixed, so the transformed grid coincides with the reference grid.",
      translation: "Infinity is fixed. Generalized circles through infinity appear as straight lines in the complex plane.",
      dilation: "Zero and infinity are fixed. The map is conformal but is not an isometry of the round-sphere metric.",
      rotation: "The subgroup \\(\\mathrm{PSU}(2)\\) also preserves the round-sphere metric.",
      special: "This is a finite \\(\\ell_1\\) flow. Its apparent pole in the complex plane is simply a regular point mapped to infinity on \\(\\mathbb{CP}^1\\).",
      loxodromic: "A loxodromic transformation has two fixed points and combines rotation with contraction and expansion along the sphere.",
    },
    wittNotes: {
      "-2": "\\(\\ell_{-2}\\) has a pole at \\(z=0\\), so its principal-branch finite flow is only a local visualization.",
      "-1": "\\(\\ell_{-1}\\) generates translations and extends to a global Möbius transformation of the Riemann sphere.",
      "0": "\\(\\ell_0\\) generates dilation for real \\(\\epsilon\\) and rotation for imaginary \\(\\epsilon\\); both are global Möbius transformations.",
      "1": "\\(\\ell_1\\) generates the Möbius transformation \\(z\\mapsto z/(1+\\epsilon z)\\).",
      "2": "\\(\\ell_2\\) is locally holomorphic, but its principal-branch finite flow is not a one-to-one automorphism of the Riemann sphere.",
    },
    flowBackward: " Because \\(\\epsilon<0\\), the finite grid flows opposite to the red arrows.",
    flowForward: " Because \\(\\epsilon>0\\), the finite grid flows along the red arrows.",
    flowZero: " At \\(\\epsilon=0\\), the grid is unchanged; the arrows still show the positive generator.",
  },

  ja: {
    title: "2 次元共形変換",
    lede: "局所微分から大域的な Möbius 変換、無限小変換の Witt 代数まで、共形対称性のつながりを調べます。",
    localTitle: "局所共形性",
    mapLabel: "写像",
    mixingLabel: "反正則混合 \\(\\lambda\\)",
    axisRatio: "軸比 \\(K\\)",
    orientationLabel: "向き",
    localCaption: "紫線は小円の厳密な像、緑の破線は Wirtinger 微分による一次近似です。共形微分では二つの局所特異値が一致します。",
    globalTitle: "Möbius 変換",
    mobiusLabel: "\\(\\mathrm{PSL}(2,\\mathbb C)\\) のプリセット",
    sphereLegend: "灰色は基準格子、青と金は変換後の緯線と経線です。赤点は Möbius 変換の固定点を表します。",
    mobiusInvariant: "<strong>不変量：</strong>一般化円は一般化円へ写り、交差角と順序付き交比は保存されます。",
    algebraTitle: "Witt 代数",
    modeLabel: "モード \\(m\\)",
    flowLabel: "フローパラメータ \\(\\epsilon\\)",
    wittCaption: "赤い矢印は正のフローパラメータに対応する生成子を示します。Riemann 球面上で大域的に正則なベクトル場は \\(m=-1,0,1\\) だけで、それ以外の有限フローは必要に応じて主値分枝を選んだ局所的な表示です。",
    chainTitle: "要点",
    chainLocal: "<strong>局所：</strong>\\(\\partial_{\\bar z}f=0\\) かつ \\(\\partial_zf\\ne0\\) なら、実 Jacobian は回転と一様な拡大縮小の積になります。",
    chainGlobal: "<strong>大域：</strong>\\(\\mathbb{CP}^1\\) の正則自己同型は \\(\\mathrm{PSL}(2,\\mathbb C)\\) をなします。",
    chainAlgebra: "<strong>代数：</strong>\\(\\ell_{-1},\\ell_0,\\ell_1\\) が大域的な Möbius 部分群を生成し、整数モード全体は局所的に中心拡張のない Witt 代数をなします。",
    orientationPreserving: "保存",
    orientationReversing: "反転",
    orientationDegenerate: "退化",
    localPlotAria: "選択した写像の前後における複素平面の格子と小円",
    spherePlotAria: "Riemann 球面上の基準座標曲線と変換後の曲線",
    wittPlotAria: "複素平面上の有限 Witt フローと無限小ベクトル場",
    localNotes: {
      square: "\\(z=0\\) から離れた点では微分が \\(2z\\) の乗算なので、十分小さな円は一次近似で円のまま写ります。",
      conjugate: "複素共役は角度を保存しますが、局所的な向きを反転します。",
      mixed: "\\(\\lambda=0\\) では正則です。\\(|\\lambda|\\) を大きくすると二つの特異値が離れ、\\(|\\lambda|=1\\) で微分が退化します。",
      exponential: "大域的には格子が曲がって重なりますが、零でない局所微分は常に回転と一様な拡大縮小になります。",
      "special-conformal": "平面座標での極を除けば、これは \\(\\ell_1=-z^2\\partial_z\\) が生成する有限フローです。",
    },
    mobiusNotes: {
      identity: "球面上のすべての点が固定され、変換後の格子は基準格子と一致します。",
      translation: "無限遠点は固定されます。無限遠点を通る一般化円は複素平面上では直線として現れます。",
      dilation: "零点と無限遠点が固定されます。共形変換ですが、丸い球面の計量に対する等長変換ではありません。",
      rotation: "部分群 \\(\\mathrm{PSU}(2)\\) は丸い球面の計量も保存します。",
      special: "これは有限 \\(\\ell_1\\) フローです。複素平面上で見かけ上現れる極は、\\(\\mathbb{CP}^1\\) 上では単に無限遠点へ写る通常の点です。",
      loxodromic: "loxodromic 変換は二つの固定点をもち、球面上での回転と収縮・膨張を組み合わせます。",
    },
    wittNotes: {
      "-2": "\\(\\ell_{-2}\\) は \\(z=0\\) に極をもつため、主値分枝による有限フローは局所的な表示に限られます。",
      "-1": "\\(\\ell_{-1}\\) は平行移動を生成し、Riemann 球面上の大域的な Möbius 変換へ拡張できます。",
      "0": "\\(\\ell_0\\) は実 \\(\\epsilon\\) に対して拡大縮小、虚 \\(\\epsilon\\) に対して回転を生成し、いずれも大域的な Möbius 変換になります。",
      "1": "\\(\\ell_1\\) は Möbius 変換 \\(z\\mapsto z/(1+\\epsilon z)\\) を生成します。",
      "2": "\\(\\ell_2\\) は局所的には正則ですが、主値分枝で表した有限フローは Riemann 球面の一対一な自己同型にはなりません。",
    },
    flowBackward: " \\(\\epsilon<0\\) なので、有限格子は赤い矢印と反対向きに流れます。",
    flowForward: " \\(\\epsilon>0\\) なので、有限格子は赤い矢印の向きに流れます。",
    flowZero: " \\(\\epsilon=0\\) では格子は変化しませんが、矢印は正の生成子を示します。",
  },
};

const t = key => MESSAGES[LOCALE][key] ?? MESSAGES.en[key] ?? key;
const pendingMathTargets = new Set();
let mathFlushScheduled = false;

function typeset(target) {
  pendingMathTargets.add(target);
  flushMath();
}

function flushMath() {
  const startup = window.MathJax?.startup?.promise;
  if (!startup || mathFlushScheduled) return;
  mathFlushScheduled = true;
  startup.then(() => {
    const targets = [...pendingMathTargets];
    pendingMathTargets.clear();
    if (!targets.length) return undefined;
    window.MathJax.typesetClear(targets);
    return window.MathJax.typesetPromise(targets);
  }).catch(() => {}).finally(() => {
    mathFlushScheduled = false;
    if (pendingMathTargets.size) flushMath();
  });
}

window.addEventListener("physics-atlas:mathjax-ready", flushMath);

let plotResizeScheduled = false;
function schedulePlotResize() {
  if (plotResizeScheduled) return;
  plotResizeScheduled = true;
  window.requestAnimationFrame(() => {
    plotResizeScheduled = false;
    for (const id of ["local-grid", "mobius-sphere", "witt-plot"]) {
      const plot = byId(id);
      if (plot?.classList.contains("js-plotly-plot")) Plotly.Plots.resize(plot);
    }
  });
}

window.addEventListener("resize", schedulePlotResize);
if ("ResizeObserver" in window) {
  const plotResizeObserver = new ResizeObserver(schedulePlotResize);
  plotResizeObserver.observe(document.querySelector("main"));
  window.addEventListener("pagehide", () => plotResizeObserver.disconnect(), {once: true});
}

function localizeStaticContent() {
  document.documentElement.lang = LOCALE;
  document.title = t("title");
  document.querySelectorAll("[data-i18n]").forEach(element => {
    element.innerHTML = t(element.dataset.i18n);
  });
  byId("local-grid").setAttribute("aria-label", t("localPlotAria"));
  byId("mobius-sphere").setAttribute("aria-label", t("spherePlotAria"));
  byId("witt-plot").setAttribute("aria-label", t("wittPlotAria"));
  typeset(document.body);
}

const LOCAL_LABELS = {
  en: {
    square: "holomorphic: f(z) = z²",
    conjugate: "antiholomorphic: f(z) = z̄",
    mixed: "nonconformal: f(z) = z + 0.4z̄",
    exponential: "holomorphic: f(z) = exp(z)",
    "special-conformal": "Möbius/SCT: f(z) = z/(1 + 0.4z)",
  },
  ja: {
    square: "正則：f(z) = z²",
    conjugate: "反正則：f(z) = z̄",
    mixed: "非共形：f(z) = z + 0.4z̄",
    exponential: "正則：f(z) = exp(z)",
    "special-conformal": "Möbius/SCT：f(z) = z/(1 + 0.4z)",
  },
};

const MOBIUS_LABELS = {
  en: {identity: "identity", translation: "translation", dilation: "dilation", rotation: "sphere rotation", special: "special conformal", loxodromic: "loxodromic"},
  ja: {identity: "恒等変換", translation: "平行移動", dilation: "拡大縮小", rotation: "球面回転", special: "特殊共形変換", loxodromic: "loxodromic変換"},
};

const MOBIUS_PARAMETER_LABELS = {
  en: {
    translation: "Translation parameter \\(s\\)",
    dilation: "Dilation parameter \\(\\rho\\)",
    rotation: "Rotation angle \\(\\theta\\)",
    special: "Special-conformal parameter \\(s\\)",
    loxodromic: "Loxodromic parameter \\(s\\)",
  },
  ja: {
    translation: "平行移動パラメータ \\(s\\)",
    dilation: "拡大縮小パラメータ \\(\\rho\\)",
    rotation: "回転角 \\(\\theta\\)",
    special: "特殊共形パラメータ \\(s\\)",
    loxodromic: "loxodromic パラメータ \\(s\\)",
  },
};

const WITT_LABELS = {
  en: {"-2": "m = −2", "-1": "m = −1 · translation", "0": "m = 0 · dilation", "1": "m = 1 · special conformal", "2": "m = 2"},
  ja: {"-2": "m = −2", "-1": "m = −1 · 平行移動", "0": "m = 0 · 拡大縮小", "1": "m = 1 · 特殊共形", "2": "m = 2"},
};

function renderLocal() {
  const key = byId("local-map").value;
  const usesMixing = key === "mixed";
  byId("mixing-control").classList.toggle("is-hidden", !usesMixing);
  const mixingIndex = Number(byId("mixing").value);
  const item = usesMixing ? DATA.localVariants[DATA.mixingKeys[mixingIndex]] : DATA.local[key];
  Plotly.react("local-grid", item.grid.data, item.grid.layout, PLOT_CONFIG);
  byId("partial-z").textContent = item.partialZ;
  byId("partial-zbar").textContent = item.partialZbar;
  byId("distortion").textContent = item.distortion === "infinite" ? "∞" : item.distortion;
  const orientationKey = `orientation${item.orientation[0].toUpperCase()}${item.orientation.slice(1)}`;
  byId("orientation").textContent = t(orientationKey);
  byId("local-note").innerHTML = t("localNotes")[key];
  byId("mixing-value").textContent = Number(DATA.mixingValues[mixingIndex]).toFixed(2);
  typeset(byId("local-note"));
  schedulePlotResize();
}

function renderMobius() {
  const key = byId("mobius-preset").value;
  const family = DATA.mobius[key];
  const parameterized = family.values.length > 1;
  byId("mobius-parameter-control").classList.toggle("is-hidden", !parameterized);
  const parameterIndex = parameterized ? Number(byId("mobius-parameter").value) : 0;
  const item = family.variants[family.keys[parameterIndex]];
  const layout = {
    ...DATA.mobiusBase.layout,
    title: {...DATA.mobiusBase.layout.title, text: item.title},
  };
  Plotly.react("mobius-sphere", [...DATA.mobiusBase.data, ...item.traces], layout, PLOT_CONFIG);
  byId("mobius-note").innerHTML = t("mobiusNotes")[key];
  byId("mobius-matrix").textContent = `\\(M\\sim\\begin{pmatrix}${item.matrix[0][0]}&${item.matrix[0][1]}\\\\${item.matrix[1][0]}&${item.matrix[1][1]}\\end{pmatrix},\\quad\\det M=1\\)`;
  if (parameterized) {
    byId("mobius-parameter-value").textContent = Number(family.values[parameterIndex]).toFixed(2);
  }
  typeset(byId("mobius-note"));
  typeset(byId("mobius-matrix"));
  schedulePlotResize();
}

function configureMobiusParameter() {
  const key = byId("mobius-preset").value;
  const family = DATA.mobius[key];
  const slider = byId("mobius-parameter");
  slider.min = 0;
  slider.max = family.values.length - 1;
  slider.step = 1;
  slider.value = family.defaultIndex;
  if (family.values.length > 1) {
    byId("mobius-parameter-label").innerHTML = MOBIUS_PARAMETER_LABELS[LOCALE][key];
    typeset(byId("mobius-parameter-control"));
  }
}

function renderWitt() {
  const mode = byId("witt-mode").value;
  const epsilonIndex = Number(byId("epsilon").value);
  const epsilon = Number(DATA.epsilonValues[epsilonIndex]);
  const figure = DATA.witt[mode][DATA.epsilonKeys[epsilonIndex]];
  Plotly.react("witt-plot", figure.data, figure.layout, PLOT_CONFIG);
  const direction = epsilon < 0 ? t("flowBackward") : epsilon > 0 ? t("flowForward") : t("flowZero");
  byId("witt-note").innerHTML = t("wittNotes")[mode] + direction;
  byId("epsilon-value").textContent = epsilon.toFixed(3);
  typeset(byId("witt-note"));
  schedulePlotResize();
}

localizeStaticContent();
byId("local-map").replaceChildren(...Object.entries(LOCAL_LABELS[LOCALE]).map(([key, label]) => new Option(label, key)));
byId("local-map").addEventListener("change", renderLocal);
byId("mixing").min = 0;
byId("mixing").max = DATA.mixingValues.length - 1;
byId("mixing").step = 1;
byId("mixing").value = DATA.mixingValues.findIndex(value => Math.abs(value - 0.4) < 1e-9);
byId("mixing").addEventListener("input", renderLocal);
byId("mobius-preset").replaceChildren(...Object.entries(MOBIUS_LABELS[LOCALE]).map(([key, label]) => new Option(label, key)));
byId("mobius-preset").addEventListener("change", () => {
  configureMobiusParameter();
  renderMobius();
});
byId("mobius-parameter").addEventListener("input", renderMobius);
byId("witt-mode").replaceChildren(...Object.entries(WITT_LABELS[LOCALE]).map(([key, label]) => new Option(label, key)));
byId("witt-mode").value = "1";
byId("witt-mode").addEventListener("change", renderWitt);
byId("epsilon").min = 0;
byId("epsilon").max = DATA.epsilonValues.length - 1;
byId("epsilon").step = 1;
byId("epsilon").value = DATA.epsilonValues.findIndex(value => Math.abs(value - 0.2) < 1e-9);
if (Number(byId("epsilon").value) < 0) byId("epsilon").value = Math.floor(DATA.epsilonValues.length / 2);
byId("epsilon").addEventListener("input", renderWitt);
renderLocal();
configureMobiusParameter();
renderMobius();
renderWitt();
