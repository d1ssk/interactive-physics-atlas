# Type specimens

This local-only page compares possible English and Japanese body typefaces without including
them in the published site.

From the repository root, serve the workbench over loopback HTTP:

```console
python -m http.server 8000
```

Then open <http://localhost:8000/workbench/type-specimens/>. The specimens use Google Fonts, so
the browser needs network access on first load.
